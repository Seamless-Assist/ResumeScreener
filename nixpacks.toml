# Nixpacks configuration for Coolify
# Bypasses setuptools compilation errors by forcing pip installation

[variables]
NIXPACKS_PYTHON_PACKAGE_MANAGER = "pip"
NIXPACKS_START_CMD = "python web/app.py"

[start]
cmd = "python web/app.py"
